# chkp-bryans.github.io

Personal site for Bryan Smith. Live at https://chkp-bryans.github.io/

Static HTML/CSS (optional JS). GitHub Pages serves `main` from the repo root.
There is no build step.

## Edit copy

Identity and project blurbs live in `index.html`. A LinkedIn `USER:` comment
is present (URL not supplied — do not invent). No email or photo in v1.

## Preview locally

```bash
python3 -m http.server 8080
```

Open http://127.0.0.1:8080/

## Deploy

Push to `main`. Pages publishes automatically (allow up to 10 minutes).
